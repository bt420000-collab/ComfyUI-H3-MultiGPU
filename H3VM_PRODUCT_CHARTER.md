# H3 VRAM Master 产品与开发总纲

> **H3VM = H3 VRAM Master**
>
> **你负责选镜头，H3VM 负责安排显卡。**

这份文件不是某一版实现说明，而是 H3VM 的长期产品与开发基线。

技术会替换，CUDA、ComfyUI、量化格式、通信方法和多卡算法都会继续变化；H3VM 的方向不应该跟着每天出现的新术语漂移。

## H3VM 到底是什么

H3 VRAM Master 是为 MiniMax H3 本地生成设计的 **显存与多显卡计算管理器**。

它不只是一个双卡 Loader，也不只是一个加速节点。

H3VM 要解决的是：

> **让整台电脑的 GPU、显存和系统内存一起服务 H3，并根据当前硬件与任务，选择最合适的执行方式。**

## 最高目标

H3VM 不追求“第二张显卡终于亮了”。

**GPU1 忙，不等于优化成功。**

一个新方案进入主线，至少应该带来一种可测量的正收益：

1. **速度收益**：更短的实际生成 wall time。
2. **容量收益**：单卡跑不了的任务，多卡能够完成。
3. **质量/精确性收益**：在相同成本下得到更可靠的计算或更稳定的长时结果。
4. **兼容性收益**：让原本无法使用多卡的普通硬件也能工作。
5. **使用成本收益**：更少改工作流、更少手调参数、更容易理解和复现。

## 产品模式，而不是论文目录

用户界面优先表达“我要得到什么”，技术术语留给日志和高级信息。

稳定的产品目标是：

- **单卡模式**：兼容与基线。
- **双卡协同**：日常稳定分工。
- **双卡扩容**：优先解决单卡显存不够。
- **双卡极速**：优先追求生成速度。
- **双卡原生**：两张卡共同完成精确模型计算。

技术实现可以换，产品目标不要轻易改名。

## 八条开发原则

1. **用户目标优先于技术名词。** 先定义用户得到什么，再选择技术。
2. **自动适配优先于固定参数。** 让 H3VM 学会用户的电脑，而不是让用户背调参表。
3. **Windows 无 P2P 是一等公民。** 能直连就直连，不能直连就通过 Host/RAM Relay 工作。
4. **不做隐藏行为。** 不允许模式借壳、静默忽略 LoRA、界面与后台契约不一致。
5. **正确性先于“看起来更快”。** Exact 路径先过 parity，近似路径必须明确标注。
6. **保留稳定回退路径。** 优化可以激进，故障策略必须保守。
7. **尽量不要求用户重做工作流。** 完整 Master Loader 与窄边界 Core 同时存在。
8. **真机数据决定晋升。** wall time、显存、transport、质量和回退都要有实际证据。

## 新技术进入主线前的五个问题

1. 它解决的是速度、容量、质量、兼容性还是使用成本？
2. 收益能不能用真实 H3 workload 测出来？
3. 它会不会破坏现有模式已经成立的用户承诺？
4. 失败时能不能安全回退？
5. 普通用户是否真的需要知道这个技术名？

如果第 5 个问题答案是“不需要”，就把它留在日志和高级设置里。

## Planner 的长期角色

H3VM 的最终形态不是越来越多的固定档位，而是一个越来越懂硬件的 Planner。

它应该逐步综合 VRAM、GPU 算力、卡间对称性、P2P、Host Relay 带宽、模型格式、分辨率、时长、步数和用户目标，然后决定工作比例、权重驻留、RAM backing、transfer policy 与后端选择。

## CPU 与 GPU 的长期分工

> **GPU 负责大规模算，CPU 负责决定什么值得算。**

CPU 应逐步承担观察、估计、策略选择、漂移判断、纠偏、数据交换决策、Host Relay 编码与安全回退。

## 公共 H3VM 与私有生产系统的边界

公开 H3VM 可以包含 VRAM 管理、多 GPU 计算、Planner、Host Relay、Attention/QKV/MLP 分片、Capacity、Mode4、Exact-SP、Dual Video VAE 与 telemetry。

任务队列、Worker Lease、工程生命周期、P1/P2/P3 调度、Artifact 路由等属于上层生产系统，不属于 H3VM 公共运行时。

## Definition of Done

一项 H3VM 功能只有同时满足下面这些条件，才算真正完成：

- 能解释它给用户带来的价值；
- 能在真实模型上运行；
- 有可比较的数据；
- 有正确性门禁；
- 有异常/回退策略；
- UI 不误导；
- 日志能诊断；
- 文档与真实行为一致；
- 不泄漏无关实验现场和私有系统语义。

## 一句话总纲

> **H3 VRAM Master 的任务不是收集多 GPU 技术，而是把一台普通电脑里能够用于 H3 的算力、显存和内存组织起来，用最合适的方法完成当前任务。**

技术每天都可以更新。

这句话不能漂。
