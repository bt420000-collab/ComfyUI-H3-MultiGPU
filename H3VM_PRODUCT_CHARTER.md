# H3 VRAM Master 产品与开发总纲

> **H3VM = H3 VRAM Master**
>
> **你负责选镜头，H3VM 负责安排显卡。**

这份文件不是某一版实现说明，而是 H3VM 的长期产品与开发基线。

技术会替换，CUDA、ComfyUI、量化格式、通信方法和多卡算法都会继续变化；H3VM 的方向不应该跟着每天出现的新术语漂移。

---

## 1. H3VM 到底是什么

H3 VRAM Master 是为 MiniMax H3 本地生成设计的 **显存与多显卡计算管理器**。

它不只是一个双卡 Loader，也不只是一个加速节点。

H3VM 要解决的是：

> **让整台电脑的 GPU、显存和系统内存一起服务 H3，并根据当前硬件与任务，选择最合适的执行方式。**

普通工作流主要描述“生成什么”。

H3VM 负责回答另一组问题：

- 模型放在哪？
- 哪张卡多算、哪张卡少算？
- 哪些权重应该留在显存？
- 哪些数据可以留在系统内存？
- 两张卡能否直连？不能直连时怎样中转？
- 当前任务应该追求速度、容量、兼容性还是精确并行？
- 新技术带来的收益是否值得它增加的复杂度？

---

## 2. H3VM 的最高目标

H3VM 不追求“第二张显卡终于亮了”。

**GPU1 忙，不等于优化成功。**

一个新方案进入主线，至少应该带来下面一种可测量的正收益：

1. **速度收益**：更短的实际生成 wall time。
2. **容量收益**：单卡跑不了的任务，多卡能够完成。
3. **质量/精确性收益**：在相同成本下得到更可靠的计算或更稳定的长时结果。
4. **兼容性收益**：让原本无法使用多卡的普通硬件也能工作。
5. **使用成本收益**：更少改工作流、更少手调参数、更容易理解和复现。

如果一项技术只能让日志更复杂、GPU 利用率更漂亮，却没有以上收益，它不应该成为主线复杂度。

---

## 3. 产品模式，而不是论文目录

用户界面优先表达“我要得到什么”，技术术语留给日志和高级信息。

| 产品模式 | 内部技术名 | 用户目标 |
|---|---|---|
| **单卡模式** | `SINGLE_GPU` | 最兼容、最清晰的基线 |
| **双卡协同** | `DUAL_QUIET` | 两张卡稳定分担日常计算 |
| **双卡扩容** | `DUAL_CAPACITY` | 联合两张卡的显存能力 |
| **双卡极速** | `DUAL_SYNC_ACCEL / Mode4` | 优先追求生成速度 |
| **双卡原生** | `DUAL_EXACT_SP` | 两张卡共同完成精确模型计算 |

技术实现可以换，产品目标不要轻易改名。

例如：

- Host Relay 是“内存中转”的一种实现。
- Exact-SP 是“双卡原生”的一种实现。
- Snapshot / Predictor 是“双卡极速”的一种实现。
- Quantized QKV shard 是“减少显存和重复计算”的一种实现。

以后出现更好的算法，应该替换实现，而不是继续往 UI 里堆第六、第七个研究术语。

---

## 4. H3VM 的八条开发原则

### 4.1 用户目标优先于技术名词

先定义用户得到什么，再选择技术。

不要因为某个新算法很新，就为了“支持它”而增加一个模式。

### 4.2 自动适配优先于固定参数

双 16GB、16GB+8GB、不同 SM 数量、不同 PCIe 拓扑，本来就不应该使用同一组硬编码比例。

长期方向是：

> **让 H3VM 学会用户的电脑，而不是让用户学习 H3VM 的调参表。**

### 4.3 Windows 无 P2P 是一等公民

普通消费级 Windows 双卡没有 NVLink / CUDA P2P，不应被当作异常边缘场景。

能直连就直连；不能直连就通过 Host/RAM Relay 工作，并根据实测带宽选择策略。

### 4.4 不做隐藏行为

界面选择的模式必须和真正运行的后端一致。

禁止：

- 用环境变量偷偷把 A 模式换成 B 模式；
- 界面显示 Turbo 4 步，后台却静默忽略 Turbo；
- 不支持的 LoRA / hook / patch 悄悄丢失。

无法保证正确时，宁可明确报错或回退，也不要假装支持。

### 4.5 正确性先于“看起来更快”

Exact / 原生计算路径必须先过 tensor parity，再谈生产加速。

近似路径必须明确告诉用户它是近似路径，并保留精确基线用于比较。

### 4.6 保留稳定回退路径

新的 packet、quant shard、relay、planner、backend 失败时，应尽可能回到已经验证的路径，而不是让整个采样崩溃。

优化可以激进，故障策略必须保守。

### 4.7 尽量不要求用户重做工作流

H3VM 应同时提供：

- 完整的一体化 Master Loader；
- 狭窄、可组合的 `MODEL -> H3VM Core -> MODEL` 执行边界。

复杂生产工作流定义“生成什么”；H3VM 定义“怎样在硬件上执行”。

### 4.8 真机数据决定晋升

一个实验技术从 Lab 晋升到公开模式，需要：

- 真实 H3 模型运行；
- 正确性/画质门禁；
- wall time；
- 两卡显存峰值；
- transport 开销；
- 至少一个稳定回退方案；
- 清晰的人话说明。

“GPU1 利用率更高”不属于单独的晋升理由。

---

## 5. 新技术进入主线前的五个问题

每次准备加入一个新算法、库、通信方式或调度器，先回答：

1. **它解决的是速度、容量、质量、兼容性还是使用成本？**
2. **收益能不能用真实 H3 workload 测出来？**
3. **它会不会破坏现有模式已经成立的用户承诺？**
4. **失败时能不能安全回退？**
5. **普通用户是否需要知道这个技术名？**

如果第 5 个问题答案是“不需要”，就把它留在日志和高级设置里。

---

## 6. Planner 的长期角色

H3VM 的最终形态不是越来越多的固定档位，而是一个越来越懂硬件的 Planner。

Planner 应逐步综合：

- VRAM 容量
- GPU 计算规模
- 卡间对称性
- CUDA P2P 可用性
- Host Relay 实测带宽
- 当前模型/量化格式
- 分辨率、时长与步数
- 当前目标：速度 / 容量 / 精确性

然后决定：

- 工作比例
- head / token / block 分配
- 权重驻留
- RAM backing
- transfer policy
- 是否值得使用某个双卡后端

用户可以保留高级手动控制，但默认路径应该越来越自动。

---

## 7. CPU 与 GPU 的分工思想

GPU 是大规模并行计算引擎，CPU 不应该只是启动器。

在 H3VM 的长期架构里，CPU 更适合承担：

- 观察运行状态
- 判断资源和风险
- 选择策略
- 估计漂移和收益
- 决定何时交换数据
- 压缩/编码 Host Relay 数据
- 进行动态调度和安全回退

也就是：

> **GPU 负责大规模算，CPU 负责决定什么值得算。**

这条原则尤其适用于 Mode4 下一代 Control Plane。

---

## 8. 公共 H3VM 与私有生产系统的边界

公开 H3VM 可以包含：

- VRAM 管理
- 多 GPU 计算
- Planner
- Host Relay / transport
- Attention / QKV / MLP 分片
- Capacity
- Mode4
- Exact-SP
- Dual Video VAE
- 性能 telemetry

但不把私有生产编排系统混进来。

任务队列、Worker Lease、工程生命周期、P1/P2/P3 调度、Artifact 路由等属于上层生产系统，不是 H3VM 的公共运行时职责。

H3VM 应保持为一个可以被其它工作流和系统调用的底层执行层。

---

## 9. Definition of Done

一项 H3VM 功能只有同时满足下面这些条件，才算真正完成：

- 能解释它给用户带来的价值；
- 能在真实模型上运行；
- 有可比较的数据；
- 有正确性门禁；
- 有异常/回退策略；
- UI 不误导；
- 日志能诊断；
- 文档与真实行为一致；
- 不泄漏无关的实验现场和私有系统语义。

---

## 10. 一句话总纲

> **H3 VRAM Master 的任务不是收集多 GPU 技术，而是把一台普通电脑里能够用于 H3 的算力、显存和内存组织起来，用最合适的方法完成当前任务。**

技术每天都可以更新。

这句话不能漂。
