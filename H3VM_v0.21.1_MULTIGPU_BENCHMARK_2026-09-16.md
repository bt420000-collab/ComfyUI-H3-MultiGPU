# H3VM Core 单卡与多卡模式基准测试报告

**版本：H3VM v0.21.1**  
**测试日期：2026-09-16**  
**平台：Windows 11 / 双 RTX 5060 Ti 16GB**

> 在 0.7MP 正式矩阵（1152×640、124 帧、20 步）中，双卡极速模式将端到端执行从 **525.6s** 缩短至 **323.1s（1.63×）**，采样器步时由 **23.80s/it** 降至 **12.81s/it**。双卡扩容模式以 **0.85×** 的速度为代价，将主卡显存峰值压低至 **6.08 GiB**，同时取得本轮多卡模式中最高的单卡基线一致性（PSNR **18.32 dB**）。

本报告记录 H3VM v0.21.1 在固定工作流、固定首帧、固定提示词和固定 Seed 下的实机测试结果。数据仅代表本次硬件、软件版本与测试矩阵，不应直接外推到所有显卡组合、分辨率、时长或模型配置。

## 1. 测试概览

本次测试以 `01_H3VM_Core_Reference_Workflow`（FL2VA 图生视频）为统一管线，在双 RTX 5060 Ti（各 16 GiB）平台上对 H3VM Core 引擎的 **1 种单卡模式 + 6 种多卡模式**进行基准对比。

每种模式分别在：

- **0.2MP 验证矩阵**：608×352
- **0.7MP 正式矩阵**：1152×640

下各运行一次。固定提示词、首帧与种子，全部 **14 次运行均成功产出 124 帧带音频视频**。

## 2. 测试环境与配置

### 2.1 硬件环境

| 项目 | 配置 |
|---|---|
| GPU | 2 × NVIDIA GeForce RTX 5060 Ti（每卡 16,311 MiB，PCIe Gen5 x8） |
| 显卡驱动 | NVIDIA 616.92 |
| CPU | Intel Core i5-14600KF（24 核：8P + 12E） |
| 内存 | 55.8 GiB |
| 操作系统 | Microsoft Windows 11 专业版 10.0.27982 |

### 2.2 软件环境

| 项目 | 版本 / 参数 |
|---|---|
| Python | 3.13.14（MSC v.1944 64 bit AMD64） |
| PyTorch | 2.13.0+cu130 |
| ComfyUI | 0.35.0 |
| H3VM 插件 | ComfyUI-H3-VRAM-Master 0.21.1（Core + Dual Video VAE） |
| 启动参数 | `--vram-headroom 2 --disable-pinned-memory` |

### 2.3 模型与矩阵

| 项目 | 内容 |
|---|---|
| UNet | `minimax_h3_fl2va_pruned_int8_convrot.safetensors`（动态显存分块，约 1.5 GB 常驻段） |
| 文本编码器 | `qwen3vl_32b_minimax_h3_bf16.safetensors`（约 49 GB，流式分块装载） |
| 视频 VAE | `minimax_h3_video_vae_fp16.safetensors`（4,965 MB） |
| 音频 VAE | MiniMaxH3AudioVAE（576 MB） |
| 0.2MP 验证矩阵 | 608 × 352 · 124 帧 · 20 步 · 0.21 MP |
| 0.7MP 正式矩阵 | 1152 × 640 · 124 帧 · 20 步 · 0.74 MP |
| 固定参数 | Seed `905556484993513`（fixed）· simple 调度器 · denoise 1.0 · 同一提示词与首帧 |

## 3. 测试方法与模式矩阵

每个矩阵先执行一次预热运行，再按固定顺序依次提交 7 种模式（S1 → D1 → … → D6）。各模式仅改变 `H3VMCoreEngine` 的引擎参数，工作流其余部分保持一致。

指标口径：

- **端到端耗时**：服务器 `Prompt executed in … seconds` 口径，包含节点内部模型装载。
- **步时（s/it）**：采样器进度事件相邻间隔的稳态均值，排除首步装载与末步收尾。
- **显存与利用率**：运行期间周期采集 `nvidia-smi`，取峰值显存与全程平均利用率。
- **输出一致性**：整段视频逐帧 RGB 汇总 MAE / RMSE / PSNR，相对同矩阵单卡基线计算。

| 代号 | 模式 | 核心参数 | 机制说明 |
|---|---|---|---|
| S1 | 单卡基线 | `SINGLE_GPU` | 全流程在 GPU0 执行，作为速度与输出一致性的基线 |
| D1 | 双卡极速·频谱 + 双卡 VAE | `DUAL_SYNC_ACCEL · spectral · dual_vae` | 主副卡同步推测执行（频谱预测器），视频 VAE 分块双卡解码 |
| D2 | 双卡极速·频谱 + 单卡 VAE | `DUAL_SYNC_ACCEL · spectral` | 同 D1，但关闭双卡 VAE，用于隔离双卡 VAE 的贡献 |
| D3 | 双卡扩容 + 双卡 VAE | `DUAL_CAPACITY · dual_vae` | 模型分层跨卡存放，压缩单卡显存峰值 |
| D4 | 双卡后台 + 双卡 VAE | `DUAL_QUIET · dual_vae` | 副卡分担部分计算，降低主卡占用与利用率 |
| D5 | 双卡极速·线性 + 双卡 VAE | `DUAL_SYNC_ACCEL · linear · dual_vae` | 同步推测执行改用线性预测器 |
| D6 | 双卡极速·频谱 + 副卡 50% | `DUAL_SYNC_ACCEL · spectral · 50%` | 副卡以 50% 参与度加入，保留部分算力与显存余量 |

## 4. 执行性能对比

端到端耗时方面，双卡极速类模式（D1/D2/D5）在两个矩阵中均显著快于单卡。分辨率提高后，加速比更接近采样器自身的理论收益。扩容（D3）与后台（D4）不以纯速度为目标，在 0.2MP 下固定开销占比较大，在 0.7MP 下则更接近单卡水平。

### 4.1 0.7MP 正式矩阵

| 模式 | 执行耗时 (s) | vs 单卡 | 步时 (s/it) | 峰值显存 GPU0/GPU1 (GiB) | 平均利用率 GPU0/GPU1 | PSNR (dB) |
|---|---:|---:|---:|---:|---:|---:|
| S1 单卡基线 | 525.6 | 1.00× | 23.80 | 7.19 / 1.30 | 89% / 4% | — |
| D1 极速·频谱+双VAE | 331.1 | 1.59× | 12.79 | 15.31 / 12.97 | 78% / 78% | 13.02 |
| D2 极速·频谱+单VAE | 332.3 | 1.58× | 12.80 | 15.34 / 12.77 | 81% / 74% | 13.02 |
| D3 扩容+双VAE | 618.2 | 0.85× | 27.51 | **6.08 / 6.84** | 50% / 36% | **18.32** |
| D4 后台+双VAE | 513.1 | 1.02× | 22.30 | 7.77 / 13.17 | 62% / 27% | 16.70 |
| D5 极速·线性+双VAE | **323.1** | **1.63×** | 12.81 | 14.05 / 12.78 | 61% / 71% | 12.95 |
| D6 极速·频谱+副卡50% | 376.0 | 1.40× | 15.46 | 14.05 / 10.69 | 81% / 49% | 12.33 |

0.7MP 下，D5 以 **323.1s / 1.63×** 获得本轮最低端到端耗时；D1/D2 为约 **331–332s / 1.58–1.59×**。D3 扩容为 **618.2s / 0.85×**，但主卡显存峰值最低。

加速主要来自采样器：双卡极速将步时从 **23.80s/it** 压缩至约 **12.8s/it（约 1.86×）**。端到端只达到约 1.6×，主要因为 VAE 解码、保存等阶段不能获得同等比例并行收益。

### 4.2 0.2MP 验证矩阵

| 模式 | 执行耗时 (s) | vs 单卡 | 步时 (s/it) | 峰值显存 GPU0/GPU1 (GiB) | PSNR (dB) |
|---|---:|---:|---:|---:|---:|
| S1 单卡基线 | 143.9 | 1.00× | 5.34 | 8.88 / 1.28 | — |
| D1 极速·频谱+双VAE | 107.8 | 1.34× | 2.66 | 13.31 / 11.25 | 13.98 |
| D2 极速·频谱+单VAE | **90.9** | **1.58×** | 2.61 | 13.40 / 11.25 | 13.98 |
| D3 扩容+双VAE | 273.5 | 0.53× | 9.32 | 7.11 / 6.32 | 17.83 |
| D4 后台+双VAE | 204.6 | 0.70× | 7.28 | 9.18 / 12.72 | 16.92 |
| D5 极速·线性+双VAE | 104.1 | 1.38× | **2.59** | 13.42 / 11.27 | 13.86 |
| D6 极速·频谱+副卡50% | 117.3 | 1.23× | 2.76 | 15.28 / 8.39 | 13.90 |

D1 是该矩阵第一个双卡运行，包含约 10s 文本编码器重装载与双卡 VAE 首次装载成本，因此端到端结果低于其稳态水平。D6 的 117.3s 同样包含切换到 50% 参与度配置后的重新装载成本。

## 5. 显存占用与 GPU 利用率

显存是多卡模式的主要权衡维度之一：

- 极速类模式会让推测分支与快照同时驻留两卡，双卡峰值合计约 25–28 GiB。
- D3 扩容通过跨卡分层，将 0.7MP 下主卡峰值压到 **6.08 GiB**。
- D4 后台将更多常驻负载转移到副卡，主卡平均利用率从单卡基线的 **89%** 降至 **62%**。
- D6 50% 参与度下，副卡平均利用率约 **49%**，与设定目标基本一致。

极速类模式 GPU0 峰值为 14.05–15.34 GiB，已接近 16 GiB 卡的可用上限。继续提高分辨率或帧数时，更适合切换扩容模式，或降低副卡参与度。

## 6. 逐节点耗时分解（0.7MP）

逐节点计时显示，采样器占总耗时约 **86–92%**，是各模式差异的主要来源。视频 VAE 解码约 31–42s，音频解码与保存合计约 5s，引擎与调度开销不足 1s。

| 节点 | S1 | D1 | D2 | D3 | D4 | D5 | D6 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 文本编码 `MiniMaxH3ImageToVideo` | —¹ | 10.31 | 10.26 | —¹ | —¹ | —¹ | —¹ |
| 采样器 `SamplerCustomAdvanced` | 488.78 | 284.64 | 284.33 | 570.57 | 470.72 | 284.54 | 330.72 |
| 视频 VAE `H3VMVideoVAEDecode` | 31.92 | 31.19 | 32.94 | 42.04 | 36.89 | 33.57 | 40.08 |
| 音频 VAE `VAEDecodeAudio` | 0.45 | 0.45 | 0.48 | 0.54 | 0.51 | 0.49 | 0.47 |
| 保存 `SaveVideo` | 4.01 | 4.06 | 3.97 | 4.34 | 4.20 | 3.98 | 4.28 |
| 引擎与调度等其他 | 0.46 | 0.32 | 0.30 | 0.66 | 0.78 | 0.45 | 0.45 |

¹ 文本编码器常驻时，该节点事件未单独捕获。D1/D2 为切换双卡配置后的首次装载，约 10.3s，属于一次性成本，后续模式复用常驻权重。

双卡 VAE 的计算收益在遥测中可以看到：0.7MP / 124 帧下被切成 7 个分块，并按 3/4 分配到两卡；D1 的解码墙钟约 14.24s，合并约 1.12s。但节点总耗时还包含 VAE 模型装载，因此在 124 帧规模下，双卡 VAE 对总时间的影响明显小于采样器多卡加速。

## 7. 输出一致性（PSNR）

输出一致性使用整段视频逐帧 RGB PSNR，衡量各多卡模式相对单卡基线的偏差。

两个矩阵的排序基本一致：

- 非推测类 D3 扩容：18.32 / 17.83 dB
- 非推测类 D4 后台：16.70 / 16.92 dB
- 推测执行类 D1/D2/D5/D6：约 12.3–14.0 dB

推测执行会改变去噪轨迹，因此 PSNR 下降属于算法路径差异，而不能单独视为执行故障。

### 预测器内部遥测

| 模式 | 预测器 | 平均 rel_l2 | 余弦相似度 |
|---|---|---:|---:|
| D1 / D2 极速·频谱 | `spectral_raw` | 0.362 | 0.935 |
| D5 极速·线性 | `linear_raw` | 0.212 | 0.977 |
| D6 极速·频谱+副卡50% | `spectral_raw` | 0.106 | 0.993 |
| D3 扩容 / D4 后台 | 不使用推测执行 | — | — |

`rel_l2` 是引擎内部对推测分支与精确计算相对偏差的估计，越低表示预测越接近精确分支。但该值与最终视频 PSNR 不构成简单单调关系，因为参与度和计算切分方式也会改变最终轨迹。

## 8. 模式选型参考

| 使用场景 | 模式 | 本轮测试依据 |
|---|---|---|
| 追求最快出片 | D5 / D2 双卡极速 | 0.7MP 下 1.58–1.63×；采样器步时接近减半 |
| 主卡显存紧张 / 更长视频 | D3 双卡扩容 | 主卡峰值 6.08 GiB；本轮多卡模式中 PSNR 最高 |
| 渲染同时使用主卡 | D4 双卡后台 | 0.7MP 总耗时接近单卡，主卡平均利用率 89% → 62% |
| 副卡需要保留余量 | D6 副卡 50% | 1.40×，副卡显存 10.69 GiB、平均利用率 49% |
| 需要严格单卡基线路径 | S1 单卡 | 作为本测试的可复现实验基线 |

这些是本次测试条件下的用途映射，不代表其它显卡、模型、分辨率和时长下必然得到相同排序。

## 9. 原始数据与产物位置

本轮原始数据保存在测试机本地，报告中的路径如下（相对 `G:\ComfyUI-MiniMax-H3`）：

| 产物 | 本地位置 |
|---|---|
| 输出视频（每模式每矩阵各 1 个，124 帧） | `ComfyUI\output\video\H3VM_ModeTest\` |
| 0.7MP 矩阵原始指标 | `logs\h3vm_results\20260916_141835_matrix_1152x640_L124_s20\summary.json` |
| 0.2MP 矩阵原始指标 | `logs\h3vm_results\20260916_134557_matrix_608x352_L124_s20\summary.json` |
| 环境快照 | `logs\h3vm_results\env.json` |
| 矩阵执行与计时采集脚本 | `logs\h3vm_queue.py` |
| 日志解析与指标汇总脚本 | `logs\h3vm_analyze.py` |
| 逐帧 PSNR 对比脚本 | `logs\vid_diff.py` |
| 参考工作流 | `短视频工作流\01_H3VM_Core_Reference_Workflow.json` |

## 10. 数据来源

- 本机实测：`h3vm_queue.py` 模式矩阵运行记录与 WebSocket 事件计时（2026-09-16，双 RTX 5060 Ti）
- ComfyUI 服务器日志：步时、模式遥测、双卡 VAE 遥测（`h3vm_analyze.py` 解析）
- `nvidia-smi` 周期采样：峰值显存与平均利用率
- `vid_diff.py` 逐帧 RGB 对比：MAE / RMSE / PSNR
- 两个矩阵的 `summary.json` 与 `env.json`

---

原始网页报告由测试管线数据静态生成，图表使用本地 ECharts。为保证 GitHub 无需额外脚本即可公开阅读，本仓库版本转换为原生 Markdown 表格与文字说明，核心数据与结论保持一致。
